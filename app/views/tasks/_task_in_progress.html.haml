%div.col-xs-12.col-md-6.task
  %div.panel.panel-default
    %div.panel-body
      %div.row
        %div.col-xs-2.text-center.no-gutter
          - if task.owner.twitter_user
            = link_to(url_for_twitter(task.owner.twitter_user)) do
              = image_tag task.owner.image_url
          - if task.owner.guest_user
            = image_tag 'guest_user.png'
        %div.col-xs-10.no-gutter
          - if task.owner.twitter_user
            = link_twitter_user_name(current_user.twitter_user)
          - if task.owner.guest_user
            %span.h5.bold
              = task.owner.username
          %br
          %span.break-word
            = task.content
    %div.panel-footer
      %div.row
        %div.col-xs-6
          %div.small.text-muted
            開始:  
            = task.started_at.strftime("%Y/%m/%d %H:%M") 
            %br
            目標: 
            = task.finish_targeted_at.strftime("%Y/%m/%d %H:%M")
        %div.col-xs-6.text-right
          - if task.owner?(current_user)
            = link_to('中断', suspend_task_path(task), class: "btn btn-primary", method: :patch)
            = link_to('完了', finish_task_path(task), class: "btn btn-primary", method: :patch)
